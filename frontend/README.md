# AiGate Frontend

独立 Vue 3 SPA 目录（对应仓库里的「前端」一侧）。

- 自有 `package.json` / Vite 构建（Plan 07a 起落地）
- 仅通过 HTTP(S) 调用 `backend` 暴露的 REST/SSE API
- 禁止依赖 Go templates、Nuxt 或 SSR 全栈回潮

当前为占位；业务控制台从 Demo 0 瘦控制台（Plan 07a）开始实现。
