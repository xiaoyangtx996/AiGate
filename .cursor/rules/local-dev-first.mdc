---
description: AiGate 当前阶段以本地开发与浏览器验证为准，不维护 Docker 制品
alwaysApply: true
---

# 验证优先级（当前阶段）

现阶段 **不维护、不验收 Docker Compose / Dockerfile**。仓库内不应新增容器化部署文件；部署方式由用户在开发结束后另行指定。

## 必须优先保证

1. **本地开发可运行**：本机 Postgres（pgvector）、`go run` api/gateway/worker、`npm run dev` 前端可按 README 启动
2. **浏览器测试无误**：控制台关键路径可在浏览器点通（登录、项目、KB、MCP、Agent、用量等）
3. **功能完整无误**：业务行为、权限、数据归因与接口约定正确；`go test` / `go vet` / `npm run build` 保持绿
4. **整理流程无误**：迁移顺序、README 本地启动步骤、Demo 点击路径自洽

## 明确不要做

- 不要新增或恢复 `docker-compose.yml`、`Dockerfile`、`.dockerignore`、compose 专用脚本
- 不要因「未跑 Docker」判定交付未完成
- 不要把生产级容器 hardening、K8s、云部署当作当前默认任务

## 交付与检查时怎么说

- 验收重点：本地启动、浏览器路径、测试/构建、功能与流程正确性
- 容器化部署标为 **后续阶段**（用户指定方式后再做）
