# AiGate Web

This directory is reserved for the separate Vue 3 SPA. It will have its own
`package.json` and consume the Go API over HTTP. It must not depend on Go
templates or server-side rendering.
