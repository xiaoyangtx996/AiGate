# AiGate Kubernetes Service 占位模板
apiVersion: v1
kind: Service
metadata:
  name: aigate
  labels:
    app: aigate
spec:
  type: ClusterIP
  selector:
    app: aigate
  ports:
    - name: http
      port: 3000
      targetPort: 3000
      protocol: TCP
